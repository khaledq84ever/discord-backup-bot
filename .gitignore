# Secrets — never commit
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Runtime data (backups, sqlite, attachments)
data/
*.db
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Drive mirror artifacts (generated per run; links map holds shareable URLs)
mirror.log
drive_links.json
